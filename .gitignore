# R session artefacts
.Rhistory
.RData
.Rproj.user/
*.Rproj

# rsconnect deployment metadata (safe to commit the app info, not tokens)
# rsconnect/

# Office temporary files
~$*.docx
~$*.xlsx

# VS Code workspace (personal IDE config)
*.code-workspace

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Python cache (build_docs script)
__pycache__/
*.pyc

# 2026-06: in-app AI translator runtime artefacts
# Never commit secrets or per-call usage logs — secrets only live in the
# shinyapps.io env-var dashboard; the usage log lives ephemerally in the
# container's working dir.
.Renviron
.env
*.env
translator_usage_log.csv
translator_history_*.json
auth_tokens.csv
